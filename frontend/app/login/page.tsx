import { PagePlaceholder } from "@/components/common/page-placeholder";
export default function LoginPage() { return <PagePlaceholder title="Log in" description="Authentication is deliberately deferred beyond the foundation phase." />; }
